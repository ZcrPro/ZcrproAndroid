CREATE TABLE splash (
  _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  text TEXT NOT NULL,
  img TEXT NOT NULL
);

select_all:
SELECT *
FROM splash;

select_by_id:
SELECT *
FROM splash
WHERE _id = ?;